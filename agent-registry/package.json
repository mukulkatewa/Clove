{
  "name": "@cloveos/agent-registry",
  "version": "0.1.0",
  "description": "Persistent agent registry — define, trigger, and manage long-lived agents",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^22.0.0"
  }
}
