#include "clove/config.hpp"

// KernelConfig is currently header-only.
// This translation unit exists so the build system always has a source file
// for clove_core, and as a future home for config parsing / validation helpers.
